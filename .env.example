DATABASE_URL=postgresql://kb:kb@localhost:5432/knowledge_base
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-to-32-random-bytes
ANTHROPIC_API_KEY=sk-ant-...
PORT=3001
# Dashboard origin(s) the API allows via CORS (comma-separated).
# The .test domain must be mapped to 127.0.0.1 first — run `make hosts` (as Administrator).
FRONTEND_URL=http://sg-help-admin.test:5173,http://localhost:5173
NODE_ENV=development
